apiVersion: 1

providers:
    - name: 'RideHailing Dashboards'
      folder: RideHailing
      type: file
      disableDeletion: false
      updateIntervalSeconds: 30
      allowUiUpdates: true
      options:
          path: /var/lib/grafana/dashboards
